import 'package:flutter/material.dart';

class ManageAcademics extends StatelessWidget {
  const ManageAcademics({super.key});

  @override
  Widget build(BuildContext context) {
    return  AppBar();
  }
}
