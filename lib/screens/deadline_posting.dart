import 'package:flutter/material.dart';

class DeadlinePosting extends StatelessWidget {
  const DeadlinePosting({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar();
  }
}
